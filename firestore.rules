rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

		// Change to not be blocked by the deadline
		// Rules will be added later
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
